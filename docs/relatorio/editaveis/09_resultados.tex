\chapter{Testes de Verificação e Validação}

\section{Características Gerais}

\textcolor{red}{Introduzir os principais pontos deste capítulo. Cada teste deve conter explicações completas que garantam sua repetibilidade:
\begin{itemize}
    \item Hipóteses levantadas
    \item Condições de contorno
    \item Resultados esperados
    \item Materiais e métodos
    \item Precisão e acurácia das medidas obtidas.
\end{itemize}
}

\section{Testes da estrutura}

\textcolor{red}{Apresentar com detalhes os testes feitos para conferir se os objetivos da estrutura foram atendidos, incluindo:}

\begin{itemize}
    \item \textcolor{red}{Imagem com todas as peças separadas da estrutura (carcaça) do veículo;}
    \item \textcolor{red}{Imagem que apresente a estrutura já montada (sem necessidade de partes internas, como eletrônica ou motores);}
    \item \textcolor{red}{Ilustração contemplando o carro montado virtualmente em alguma ferramenta de CAD, no intuito de verificar se as peças foram construídas e montadas em conformidade com o projeto.}
\end{itemize}

\section{Testes de \textit{hardware}}

\textcolor{red}{Apresentar com detalhes os testes feitos para conferir se os objetivos do \textit{hardware} foram atendidos, incluindo:}

\begin{enumerate}
    \item \textcolor{red}{Processamento (Arduino, ESP32 etc):}
    \begin{itemize}
        \item \textcolor{red}{Definir interface de desenvolvimento;}
        \item \textcolor{red}{Rodar exemplo mínimo.}
    \end{itemize}
    \item {Sensores (temperatura, pressão, posição, velocidade etc.):}
\begin{itemize}
\item {Realizar leitura básica de dados;}


    Foi implementada a leitura básica dos sensores infravermelhos digitais com LM393 utilizando a ESP32 em linguagem C com ESP-IDF. As leituras são realizadas através das portas GPIO configuradas como entradas digitais, permitindo detectar a presença ou ausência de obstáculos - saída igual a 0 indica presença, saída igual a 1 indica ausência. Os dados são enviados via UART para monitoramento serial em tempo real.

    \item {Validar/calibrar leituras;}
    
    A validação dos sensores foi realizada aproximando obstáculos em diferentes distâncias para verificar a alteração do nível lógico na saída digital. A calibração foi feita ajustando o potenciômetro presente no módulo LM393, permitindo configurar a sensibilidade e distância de detecção de acordo com a necessidade do robô. Devido ao tamanho do chassi, e priorizando a posição central da célula, a calibração obtida foi a mínima possível. Isto porque o objetivo foi detectar o menor espaço dentro da faixa de operação.
    \item {Definir taxas de amostragem e quantidade de dados para transmissão e/ou armazenamento;}
    
    Inicialmente foi definida uma taxa de amostragem de aproximadamente 500 ms entre leituras. Essa configuração foi utilizada para facilitar a validação do funcionamento dos sensores e a observação dos dados transmitidos via UART.

    \item {Validar taxas definidas e quantidade de dados;}
    
    A taxa de amostragem definida mostrou-se suficiente para validação funcional do sistema e monitoramento das leituras em bancada. Entretanto, observou-se que aplicações de navegação autônoma exigirão taxas de leitura mais rápidas para melhorar o tempo de resposta na detecção de obstáculos.

    \item {Realizar processamento básico de dados (\textit{thresholding}, filtro média-móvel etc.).}
    
    O sistema utiliza processamento básico baseado em \textit{thresholding} digital realizado pelo comparador LM393, que converte automaticamente a leitura infravermelha em níveis lógicos binários. Futuramente pretende-se implementar técnicas simples de filtragem e \textit{debounce} para reduzir oscilações e falsos acionamentos durante a movimentação do robô.
\end{itemize}
```

    \item \textcolor{red}{Armazenamento (memória interna, cartão SD, pendrive etc.):}
    \begin{itemize}
        \item \textcolor{red}{Armazenar dados básicos;}
        \item \textcolor{red}{Armazenar e validar dados de sensores.}
    \end{itemize}
    \item \textcolor{red}{Atuadores (Motores DC, relés, LEDs etc.):}
    \begin{itemize}
        \item \textcolor{red}{Realizar acionamentos básicos;}
        \item \textcolor{red}{Realizar acionamentos com base em dados de sensores;}
        \item \textcolor{red}{Realizar processamento básico de dados (PWM etc.).}
    \end{itemize}
    \item \textcolor{red}{Comunicações (UART, I2C, SPI, USB, WiFi, Bluetooth etc.):}
    \begin{itemize}
        \item \textcolor{red}{Transmissão básica de dados;}
        \item \textcolor{red}{Transmissão de dados de sensores dentro da taxa de amostragem.}
    \end{itemize}
\end{enumerate}

\section{Testes de consumo energético}

\textcolor{red}{Apresentar com detalhes os testes feitos para conferir se as demandas energéticas do projeto foram atendidas.}

\section{Testes de \textit{software}}
\label{sec:testes_software}

O \textit{backlog} do produto definido no primeiro ponto de controle (PC1) é composto por seis histórias
de usuário (HU01--HU06). Nesta entrega foram efetivamente implementadas e validadas
por testes automatizados as quatro histórias do módulo de \textit{software}
(web/\textit{backend}): HU03, HU04, HU05 e HU06, o que corresponde a $66{,}7\%$ do
\textit{backlog} funcional (acima do mínimo de $50\%$). As histórias HU01
(resolução autônoma) e HU02 (mapeamento incremental) pertencem ao módulo embarcado
e dependem da integração final com a placa ESP32, estando planejadas para a etapa
seguinte; por isso não compõem o conjunto validado nesta seção.

\begin{table}[H]
    \centering
    \caption{Associação entre histórias de usuário implementadas, protótipos e
    funcionalidades acessadas na interface gráfica.}
    \label{tab:hu_prototipo_gui}
    \footnotesize
    \begin{tabular}{|c|p{3.6cm}|c|p{3.6cm}|p{3.4cm}|}
        \hline
        \textbf{HU} & \textbf{História de usuário} & \textbf{Protótipo} &
        \textbf{Funcionalidade acessada na GUI} &
        \textbf{Suíte de testes que valida} \\ \hline

        HU03 & Telemetria em tempo real: trajeto, tempo, velocidade média e
        bateria durante a corrida. & Figura~\ref{fig:telemetria_fig} &
        Tela \textit{Telemetria ao Vivo}: grade do labirinto,
        métricas e status de conexão atualizados via WebSocket. &
        Unitário (\textit{Telemetry}, \textit{useTelemetry}); E2E
        \texttt{telemetria.spec.js}; \textit{backend} POST \texttt{/telemetria}
        e WS \texttt{/ws/telemetria}. \\ \hline

        HU04 & Persistência e consulta de histórico de corridas com filtro por
        tamanho. & Figura~\ref{fig:historicocorridas_fig} &
        Tela \textit{Histórico}: tabela de corridas
        e filtro por dimensão do labirinto. &
        Unitário (\textit{History}, \textit{useHistory}); E2E
        \texttt{history.spec.js}; \textit{backend} GET \texttt{/corridas}. \\ \hline

        HU05 & Monitoramento do nível de bateria no painel web. &
        Figura~\ref{fig:monitoramentobateria_fig} &
        Indicador de bateria na tela \textit{Telemetria ao Vivo}, alimentado pelo 
        campo \texttt{nivel\_bateria} da telemetria. &
        Coberta pelos testes de telemetria (unitário e E2E); validada com dados
        simulados. \\ \hline

        HU06 & Indicador de desafio cumprido (Sim/Não) ao atingir o centro do
        labirinto. & Figura~\ref{fig:desafiocumprido_fig} &
        Campo \textit{Desafio Cumprido} na tela \textit{Telemetria ao Vivo}, 
        calculado ao atingir a região central. &
        Unitário (\textit{Telemetry}); E2E \texttt{telemetria.spec.js}
        (casos ``Sim'' e ``Não''). \\ \hline
    \end{tabular}
\end{table}

A estratégia de testes do \textit{software} cobre três níveis: testes unitários e de
integração (de componentes e de rotas), com medição de cobertura de código-fonte, e
testes de sistema do tipo funcional (\textit{end-to-end}, E2E), que exercitam a
aplicação pela interface gráfica. Todas as suítes são executadas automaticamente a
cada \textit{push} ou \textit{pull request} por um \textit{pipeline} de Integração
Contínua (GitHub Actions), que reprova a alteração caso a cobertura fique abaixo de
$70\%$.

\subsection{Testes unitários e de integração}

No \textit{backend} (FastAPI), os testes foram escritos com \texttt{pytest} e
\texttt{pytest-cov}, empregando banco SQLite em memória e \textit{fixtures} que
substituem a sessão de banco real, de modo a não depender de infraestrutura externa.
No \textit{frontend} (React), foram utilizados \texttt{Vitest} e \textit{Testing
Library} sobre o ambiente \texttt{jsdom}. A Tabela~\ref{tab:suites_unit} resume as
suítes e a cobertura obtida, ambas acima do mínimo exigido de $70\%$; os relatórios
de cobertura correspondentes constam do Apêndice~\ref{apendice_evidencias_testes}
(Figuras~\ref{fig:cobertura_backend} e~\ref{fig:cobertura_frontend}).

\begin{table}[H]
    \centering
    \caption{Suítes de testes unitários e de integração e cobertura de código.}
    \label{tab:suites_unit}
    \small
    \begin{tabular}{|l|p{3.0cm}|p{5.2cm}|c|c|}
        \hline
        \textbf{Camada} & \textbf{Ferramentas} & \textbf{Principais suítes} &
        \textbf{Testes} & \textbf{Cobertura} \\ \hline
        \textit{Backend} & \texttt{pytest}, \texttt{pytest-cov} &
        telemetria (POST e WebSocket), \textit{models} e \textit{schemas},
        histórico/consulta e \textit{health} & 74 & $98\%$ \\ \hline
        \textit{Frontend} & \texttt{Vitest}, \textit{Testing Library} &
        páginas \textit{Telemetry} e \textit{History}, componentes \textit{Layout} e
        \textit{MazeMap}, \textit{hooks} e roteamento & 37 & $93\%$ \\ \hline
    \end{tabular}
\end{table}

\subsection{Testes de sistema (E2E)}

Os testes de sistema foram automatizados com \texttt{Playwright}, executados em modo
\textit{headless} (compatível com a Integração Contínua). As suítes automatizam os
casos funcionais de \textit{software} do roteiro de testes
(Apêndice~\ref{apendice_roteiro_detalhado}); os casos do módulo embarcado (TF01 e
TF02) dependem da integração com a ESP32 e serão validados posteriormente. A suíte
completa soma 19 casos. As suítes que exercitam o fluxo de telemetria, por validarem
a integração entre os módulos, são detalhadas na Seção~\ref{sec:testes_integracao};
as suítes funcionais da aplicação web (consulta de histórico e navegação) constam da
Tabela~\ref{tab:suites_e2e}.

\begin{table}[H]
    \centering
    \caption{Suítes de testes de sistema (E2E) automatizadas com Playwright.}
    \label{tab:suites_e2e}
    \small
    \begin{tabular}{|l|p{9.3cm}|}
        \hline
        \textbf{Suíte} & \textbf{Cenários validados} \\ \hline
        \texttt{history.spec.js} & Consulta do histórico e filtro por tamanho de
        labirinto, incluindo a restauração da listagem completa (4 casos). \\ \hline
        \texttt{smoke.spec.js} & Carregamento básico da aplicação e navegação inicial
        entre as telas (2 casos). \\ \hline
    \end{tabular}
\end{table}

A execução das suítes, a verificação do limiar de cobertura e a coleta das evidências
(relatórios de cobertura e do Playwright) são realizadas automaticamente pelo
\textit{pipeline} de Integração Contínua, garantindo que a \textit{branch} principal
permaneça sempre com os testes verdes. O relatório de execução dos testes E2E é
apresentado na Figura~\ref{fig:e2e_playwright} (Apêndice~\ref{apendice_evidencias_testes}).

\section{Testes de integração}
\label{sec:testes_integracao}

A integração da comunicação de telemetria foi validada entre os componentes de
\textit{software} (API, persistência, WebSocket e \textit{frontend}), com a fonte de
dados simulada por um dublê de WebSocket e um cliente de teste que reproduzem o
contrato \texttt{TelemetriaOut}. A integração com o \textit{firmware} real da ESP32 está
planejada para a \textit{sprint}-5 (\#22 e \#23).

O fluxo de telemetria é o principal ponto de integração do sistema, pois atravessa
todas as camadas do \textit{software}: o sistema embarcado, a API, o banco de dados, o
canal de tempo real e a interface web. Por isso, esta seção concentra a verificação
desse caminho de ponta a ponta. O fluxo é o seguinte: um emissor de telemetria (um
simulador em \textit{Python} que mimetiza o \textit{firmware}, ou um dublê de WebSocket
executado no navegador) envia pacotes ao \textit{endpoint} \texttt{POST /telemetria}; o
\textit{backend} valida o pacote, persiste o evento, resolve a corrida associada
(criando-a no primeiro pacote e reutilizando-a nos seguintes) e repassa o dado, em
tempo real, a todos os clientes conectados pelo canal \texttt{/ws/telemetria}; por fim,
a interface web recebe o evento e atualiza o painel. Esse fluxo corresponde ao diagrama
de sequência da corrida completa apresentado no capítulo de projeto do sistema.
% TODO: substituir pela referência \ref{} ao diagrama de sequência quando a Tarefa
% de diagramas UML (#21) for concluída.

A verificação cobre as quatro etapas desse contrato, da ingestão do pacote até o
consumo pela interface, combinando testes de integração de \textit{backend} (que
exercitam diretamente a API, a persistência e o \textit{broadcast}) com testes de
sistema fim a fim. Ao todo, treze casos automatizados de E2E (doze do fluxo ao vivo e um
de robustez de reconexão) somam-se aos testes de integração de \textit{backend} para
cobrir o caminho completo da telemetria. A Tabela~\ref{tab:testes_integracao} detalha
cada teste, os componentes que ele integra e os cenários validados.

\begin{table}[H]
    \centering
    \caption{Testes de integração do fluxo de telemetria, validados com dados simulados.}
    \label{tab:testes_integracao}
    \small
    \begin{tabular}{|p{4.2cm}|p{3.6cm}|p{5.2cm}|}
        \hline
        \textbf{Teste / nível} & \textbf{Componentes integrados} &
        \textbf{Cenários validados} \\ \hline
        Ingestão (\texttt{POST /telemetria}, \textit{backend}) &
        API e camada de persistência (banco de dados) &
        Pacote aceito (\texttt{201}); evento de telemetria gravado; corrida criada no
        primeiro pacote e reutilizada nos seguintes. \\ \hline
        \textit{Broadcast} (WebSocket \texttt{/ws/telemetria}, \textit{backend}) &
        API, gerenciador de conexões e múltiplos clientes &
        Repasse do pacote a todos os clientes conectados; descarte de conexões inativas
        sem afetar os demais; tratamento de \textit{payload} inválido e de
        \texttt{labirinto\_id} inexistente. \\ \hline
        Fim a fim ao vivo (\texttt{telemetria.spec.js}, E2E) &
        WebSocket e \textit{frontend} (painel) &
        Atualização em tempo real de posição, velocidade média, bateria e tempo;
        renderização do \textit{grid}; indicador de desafio cumprido; robustez a
        mensagens inválidas (12 casos). \\ \hline
        Robustez de conexão (\texttt{reconexao.spec.js}, E2E) &
        WebSocket e \textit{frontend} &
        Sinalização da perda de conexão, reconexão automática e retomada das
        atualizações na interface (1 caso). \\ \hline
        Emissor $\rightarrow$ \textit{backend} (simulador de armazenamento) &
        \textit{Firmware} simulado e API &
        Envio ao \texttt{POST /telemetria} dos dados persistidos localmente, com
        criação e consulta da corrida resultante. \\ \hline
    \end{tabular}
\end{table}

Os resultados de execução dessas suítes constam do relatório de testes E2E
(Figura~\ref{fig:e2e_playwright}) e do relatório de cobertura do \textit{backend}
(Figura~\ref{fig:cobertura_backend}), ambos no
Apêndice~\ref{apendice_evidencias_testes}.

A integração completa com o \textit{hardware} físico (o envio dos pacotes pela
ESP32 a partir das leituras reais dos sensores) será validada na \textit{sprint}-5
(\#22 e \#23), quando a placa estiver plenamente configurada. Nesta entrega, a
comunicação foi verificada de ponta a ponta com dados simulados, garantindo que o
contrato de integração entre os módulos está correto.

%\textcolor{red}{Com relação ao \textit{software}, será necessário apresentar pacotes de componentes de \textit{software}, suas funções e características, e explicar as decisões de projeto.}

